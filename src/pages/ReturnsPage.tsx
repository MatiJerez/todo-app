import React from 'react';

const ReturnsPage: React.FC = () => {
  return (
    <section className="wrapper-component" aria-labelledby="returns-title">
      <h3 id="returns-title" className="d-flex title-page">
        Mis devoluciones
      </h3>
      {/* Aquí se pueden agregar futuras funcionalidades, como una lista de devoluciones */}
    </section>
  );
};

export default ReturnsPage;
